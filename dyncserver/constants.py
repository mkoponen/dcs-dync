# We restrict each number to a maximum of 99. Implicitly contains 4 numbers, and if any are omitted, they are assumed 0.
# You can also have -postX versions, like 1.0.0.0-post3 but they absolutely must retain backwards-compatibility to their
# original version, 1.0.0.0 in this example. If you break backwards-compatibility, always increment a number.
app_version = "0.1.7"
backwards_compatibility_min_version = "0.1.7"
